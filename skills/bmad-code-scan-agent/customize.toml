[skill]
name = "bmad-code-scan-agent"
description = "Fast deterministic code scanner. Performs static analysis and produces structured reports (Excel/JSON)."
version = "1.0.0"

[skill.tools]
required = ["claude-code"]

[skill.activation]
keywords = ["scan", "static analysis", "code scan", "quick scan", "lint", "violations"]

[skill.scripts]
dispatcher = "scripts/run.py"
requirements = "scripts/requirements.txt"

[skill.commands]
scan = "python3 scripts/run.py"
scan-quick = "python3 scripts/run.py --quick"
list-engines = "python3 scripts/run.py --list-engines"
